Hello. 위키 연습장 입니다.

## markdown 연습.

* 첫번째
* 두번째
 - 두번째의 첫번째

## 그냥 써봄
1. 이것은

    무엇인가

    문단이 이어지나?

2. *도 되고 +도 되나?

    1. 안으로 어떻게 들여쓰냐?
    2. 이렇게 하면 되니?
+ 흐음...
+ 하아 ...

[다음](http://www.daum.net "다아으음")


~~~ { .python }
for x in range(10):
    print x
~~~

## FESTIVAL (Python 3.3)
~~~ { .python }
from itertools import accumulate, islice

def solve(N, L, costs):
    return min(cost_total / n
        for i in range(N-L+1)
        for n, cost_total
           in enumerate(islice(accumulate(costs[i:]), i+L-1, None), L)
    )

def main():
    C = int(input())
    for _ in range(C):
        N, L = map(int, input().split())
        costs = tuple(map(int, input().split()))
        print(solve(N, L, costs))
~~~

## link
[link](http://kr.battle.net/d3/ko/profile/falsetru-3184/hero/8504765)

[Algospot Accounts](http://jsfiddle.net/deFUa/4/embedded/result/)